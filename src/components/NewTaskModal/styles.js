import styled from "styled-components";

export const Container = styled.div`
  position: relative;

  h2 {
    color: var(--title);
    font-size: 1.4rem;
    text-align: center;

    margin-bottom: 1.5rem;
  }
`;