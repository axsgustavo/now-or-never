import styled from "styled-components";

export const Container = styled.h2`
  width: fit-content;
  max-width: 500px;

  color: var(--title);
  font-weight: 400;
  font-size: 3rem;

  line-height: 3.2rem;
  text-align: center;
`;